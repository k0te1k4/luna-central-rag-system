services:
  luna-backend:
    build:
      context: ./backend
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - luna_data:/data
    restart: unless-stopped

volumes:
  luna_data:
